export enum KEYCODE {
    Enter = 13,
    ArrowUp = 38,
    ArrowDown = 40,
    ArrowLeft = 37,
    ArrowRight = 39
}

export const EMPTY_ARRAY = Object.freeze([]);

export enum TypesTag {
    StringTag = '[object String]'
}